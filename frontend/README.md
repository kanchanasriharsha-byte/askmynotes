# AskMyNotes

A full-stack application with backend and frontend services containerized using Docker.

## Project Structure
- `/backend` - Node.js / Express API
- `/frontend` - React application
- `docker-compose.yml` - Multi-container setup
-